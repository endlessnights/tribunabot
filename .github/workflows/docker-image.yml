name: Docker Image CI

on:
  push:
    branches: ["master"]

jobs:
  docker:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Update ENVs
        run: |
          source ~/.bashrc

      - name: Deploy to host
        run: |
          cd core
          docker compose up -d
